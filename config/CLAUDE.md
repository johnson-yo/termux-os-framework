# Configuration directory contract

This directory contains public, non-secret defaults only. Defaults must be device-neutral, bind to loopback, declare no product integration, and contain no credential. Persistent user configuration is created outside the repository and is never overwritten during updates.
