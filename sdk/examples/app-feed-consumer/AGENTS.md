# Example Package instructions

This is a testable SDK example, not a production application. Consume Capability descriptors and feeds without naming a provider implementation.

The worker uses `TERMUX_OS_FRAMEWORK_URL` and `TERMUX_OS_SYSTEM_KEY`; its WebUI
uses the shared Browser Session and never stores a credential.
