# Example Package instructions

This is a synthetic SDK asset example. It must never contain a real model, vendor artifact, private path, or restricted data.

Keep the payload immutable, declare its target and checksums when replacing the
fixture, and use the shared Browser Session for the Package page.
