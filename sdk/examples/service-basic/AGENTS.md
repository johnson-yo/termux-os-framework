# Example Package instructions

This is a testable SDK example, not a production service. Keep it provider-neutral, deterministic, self-contained, and free of credentials or device assumptions.

The direct HTTP API must use its manifest-assigned port and the injected System
Key. Its WebUI must use the shared Browser Session and remain portrait-phone-first.
