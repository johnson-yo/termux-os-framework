# Example Package instructions

This is a testable SDK example, not a production adapter. Use only a local fixture endpoint and never add a real credential, vendor SDK, or device address.

Keep the Package page under the Admin Adapters group and use the shared Browser
Session for same-origin Framework APIs.
