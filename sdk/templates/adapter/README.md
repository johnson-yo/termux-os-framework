# Adapter Package template

Use this template to expose an application, device, engine, or external API
through replaceable Capability contracts. Keep provider credentials in private
configuration and register the Package page under the Admin Adapters group.
