# Fixture package instructions

This package is an isolated Framework test fixture. Keep its manifest, entry
points, and release-facing documentation minimal so the runtime-contract smoke
tests remain deterministic.
