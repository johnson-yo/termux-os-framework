# Capability contract

Consumers request stable Capability IDs. Providers are discovered from installed Package manifests and may be rebound without changing consumers. Core stores descriptors and bindings; implementations remain in Extensions.
