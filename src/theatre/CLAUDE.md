# Theatre contract

Theatre is a generic Action registry and sequential, fail-fast scene runner. Core provides only an echo diagnostic. Product scenes and device Actions belong to Extension Packages; Theatre must not contain audio, TTS, translation, or hardware behavior.
